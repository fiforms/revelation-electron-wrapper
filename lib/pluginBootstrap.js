// Plugins may not auto-enable themselves via manifest or module attributes.
// The default enabled plugin list is hard-coded in configManager.js.
// New plugins installed by the user must be enabled manually in Settings.

module.exports = {};
